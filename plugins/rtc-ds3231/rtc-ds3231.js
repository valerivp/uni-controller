'use strict';

const basedir = require('path').dirname(process.mainModule.filename);

const utils = require(`${basedir}/uc-utils`);
const mm = require(`${basedir}/uc-mm`);
const router = require(`${basedir}/uc-router`);

